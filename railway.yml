services:
  - image: 'railway/java'
    env:
      - key: SRING_DATASOURCE_URL
        value: 'jdbc:mysql://host.docker.internal:3306/banco'
      - key: SRING_DATASOURCE_USERNAME
        value: 'root'
      - key: SRING_DATASOURCE_PASSWORD
        value: 'ermne12345'

build:
  - ./mvnw clean package -DskipTests

run:
  - java -jar target/web-app-bancario-0.0.1-SNAPSHOT.jar
