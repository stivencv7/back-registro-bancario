plugins:
  - image: 'railway/java'

build:
  - ./mvnw clean package -DskipTests

run:
  - java -jar target/web-app-bancario-0.0.1-SNAPSHOT.jar
  - SRING_DATASOURCE_URL=jdbc:mysql://localhost:3306/banco
  - SRING_DATASOURCE_USERNAME=root
  - SRING_DATASOURCE_PASSWORD=ermne12345